# 🚀 AI Code Generation Guidelines for Asset Paths

이 지침은 로컬 루트 개발 환경(`localhost`)과 실서버 프록시 환경(특정 하위 디렉토리 배포) 간의 리소스 로딩 에러(404)를 방지하기 위한 범용 핵심 규칙입니다. 향후 모든 작업 시 **반드시** 이 규칙을 준수하여 코드를 작성하세요.

## 🚨 [CRITICAL] 에셋(Asset) 경로 작성 규칙

1. **`<base>` 태그 사용 절대 금지**
   - HTML 내에 `<base href="...">`를 하드코딩하지 마세요. 특정 환경에 고정된 기반 경로를 사용하면 로컬과 실서버 중 하나에서는 반드시 파일 연결이 끊어집니다.

2. **모든 파일(CSS, JS, 이미지)은 '순수 상대 경로'로만 연결할 것**
   - 최상위 슬래시(`/`)나 점 슬래시(`./`)가 없는 가장 단순한 파일명 중심의 경로를 사용하세요.
   - ❌ 오답: `href="/style.css"` (최상위 루트 탐색으로 로컬 보조 서버 등에서 404 발생)
   - ❌ 오답: `href="./style.css"` (프록시 설정이나 후행 슬래시에 따라 해석이 달라짐)
   - ✅ 정답: `href="style.css"`, `src="game.js"`

위 규칙을 지키면 브라우저가 현재 URL 주소를 기준으로 알아서 올바른 경로를 매핑하므로, 로컬 테스트와 서버 프록시 환경을 오가도 소스 코드 수정 없이 100% 동일하게 동작합니다.
