# 🚀 Flag Blue / Red Project AI Guidelines

이 프로젝트는 로컬 서버(localhost:3000)와 Nginx Proxy Pass(하위 경로 `https://domain.com/flag_blue_red/` 등) 실서버 환경 두 곳 모두에서 소스 수정 없이 동일하게 배포되어야 하는 정적 HTML 프로젝트입니다.

따라서 AI(Claude, Gemini 등)는 향후 코드를 수정하거나 기능을 추가할 때 다음 규칙을 **반드시** 준수해야 합니다.

## 🚨 [CRITICAL] 경로(Path) 및 에셋(Asset) 로딩 규칙

1. **`<base>` 태그 사용 금지 (DO NOT USE `<base href="...">`)**
   - `<base href="/flag_blue_red/">` 와 같이 하드코딩된 Base 태그를 사용하면 로컬(`npx serve`) 환경에서 CSS/JS를 찾지 못합니다(404 에러).
   - 반대로, 실서버용으로 억지로 경로를 맞추려고 코드를 반복 수정하는 핑퐁 현상이 생깁니다.
   - **결론: `index.html`에서 `<base>` HTML 태그를 절대 사용하지 마세요.**

2. **모든 에셋(CSS, JS, 이미지)은 '순수 상대 경로'로만 참조할 것**
   - ❌ 잘못된 예: `href="/style.css"` (최상위 루트 기준 탐색으로 404 발생)
   - ❌ 잘못된 예: `href="./style.css"` (상황에 따라 경로 해석 충돌 가능성 있음)
   - ✅ 올바른 예: `href="style.css"`, `src="game.js"` (슬래시 마크 없이 파일명만 기재)
   
   이러한 순수 상대 경로를 사용하면, 로컬에서는 `localhost:3000/style.css`로, 실서버에서는 `domain.com/flag_blue_red/style.css`로 브라우저가 현재 열려있는 URL의 디렉토리를 기준으로 똑똑하게 맵핑합니다.

3. **Nginx 서버 Nginx 설정 원칙 (DO NOT fix via code)**
   - 만약 `<base>` 태그 없이 상대 경로를 썼는데 실서버에서 CSS가 깨진다면, 이는 사용자가 브라우저에 `domain.com/flag_blue_red` (끝에 `/` 슬래시 없음) 형태로 접속해서 브라우저가 `domain.com`을 Root로 착각했기 때문입니다.
   - 이 경우 **HTML 코드를 고치는 대신, Nginx 서버 설정으로 해결해야 함**을 인지하세요. Nginx에서 넘어오는 Proxy Pass 주소에 후행 슬래시(Trailing Slash)를 항상 보장하도록 사용자의 서버 설정을 안내하십시오.
   - *Nginx 권장 설정:*
     ```nginx
     location /flag_blue_red {
         # 끝에 슬래시가 없다면 달아주도록 리다이렉트
         rewrite ^([^.]*[^/])$ $1/ permanent;
         proxy_pass http://localhost:포트번호/;
     }
     ```

## 🎨 UI / Design (Premium Tailwind)
- 이 프로젝트는 `supanova-design-skill` 가이드를 따릅니다.
- 커스텀 CSS 파일(`style.css`) 내부에 `@apply` 등 Tailwind 문법을 작성하지 마세요 (CDN 환경에서는 컴파일되지 않음). 모든 Tailwind 클래스는 HTML 태그 내부에 직접 작성하거나 `<style type="text/tailwindcss">` 블록 안에 넣어야 합니다.
- 한국어 텍스트는 항상 `break-keep-all (word-break: keep-all)` 규칙을 적용합니다. 
